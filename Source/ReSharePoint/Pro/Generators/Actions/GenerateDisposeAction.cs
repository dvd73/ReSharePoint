namespace ReSharePoint.Pro.Generators.Actions
{
    /*
     [Action("Generate.Dispose", "Dispose", Icon = typeof(PsiFeaturesUnsortedThemedIcons.FuncZoneGenerate), Id = 11003)]
    public class GenerateDisposeAction : GenerateActionBase<GenerateDisposeItemProvider>
    {
         //public bool Update(IDataContext context, ActionPresentation presentation, DelegateUpdate nextUpdate)
         //{
         //    return true;
         //}

         //public void Execute(IDataContext context, DelegateExecute nextExecute)
         //{
         //    MessageBox.Show(
         //     "Hello",
         //     "This is Dispose action",
         //     MessageBoxButtons.OK,
         //     MessageBoxIcon.Information);
         //}

         protected override RichText Caption
         {
             get
             {
                 return new RichText("Generate Dispose");
             }
         }

         protected override void Execute(IDataContext context, IGenerateActionWorkflow workflow)
         {
             base.Execute(context, workflow);
         }

         public override void Execute(IDataContext dataContext, DelegateExecute nextExecute)
         {
             base.Execute(dataContext, nextExecute);
         }
    }
     * */
}
