<h3>testimonial</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["name"], "testimonial/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>