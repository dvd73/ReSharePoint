<h3>contactInfo</h3>
<ul class="list">
    @foreach (var item in Model)
      {
        <li>@Html.FrontHtml().PageLink(item["title"], "contactInfo/detail", new { userKey = item["UserKey"] }, new { @class="title" })
        </li>
      }
</ul>